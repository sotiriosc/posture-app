# Production Prescription Source Event Identity

Generated deterministically from the production Prescription Compiler kernel.

Algorithm: deterministic token over `sessionIntentId + assignmentHandoffId + executionAttemptId`.

Dose, rank, labels, time, muscles, satisfied needs, sides, blocks, and revisions are excluded. Revisions retain the same source event. Stress duplication count: `0`.
