# Session Practice Recommendation and Selection Boundary

Recommendation authority is `SUGGESTION_ONLY`; `automaticSelection` is always false. The default
selected mode is Full. A difficult session, pain label, or legacy `NextSessionRecommendation` cannot
force a V2 mode. Recommendations do not persist as global preferences. TrainingSafety may make all
three options unavailable.
