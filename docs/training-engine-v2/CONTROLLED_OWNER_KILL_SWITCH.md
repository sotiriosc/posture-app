# Controlled Owner Kill Switch

Mode `off` blocks new access, generation, preview and apply. An active attempt may complete safely under exact persisted truth or enter a controlled disabled state before legacy navigation. It is never stranded; no data is deleted.

Design-only. Owner delivery and Product activation counts remain `0`. Exact next dependency: `CONTROLLED_OWNER_ACCOUNT_GET_STRONGER_GOAL_DELIVERY_IMPLEMENTATION_V1_AUTHORIZATION`.
