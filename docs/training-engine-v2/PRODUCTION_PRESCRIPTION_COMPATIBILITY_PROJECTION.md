# Production Prescription Compatibility Projection

Generated deterministically from the production Prescription Compiler kernel.

States: `single_uniform_dose_compatible`, `legacy_single_dose_projection_available`, `ordered_blocks_required`, and `no_truthful_single_dose_projection`.

One block may project when rest and requirements remain truthful. Multi-block plans never flatten. Stress flattening count: `0`.
