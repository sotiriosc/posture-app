# Full Program Adaptation Persistence

Generated deterministically from explicit CAGT Authority Registry V2 and test-only Gate 14 tooling.

Observed persistence rate: `0.8`. All five explicit downstream-erasure mutations return `MATERIAL_ADAPTATION_ERASED_DOWNSTREAM`.
