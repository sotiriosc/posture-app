# Controlled Product Shadow Training Mode Mapping V2

`build -> develop` without selecting strength or hypertrophy. `maintain -> maintain` with `MAINTENANCE_WEEK_AND_LONGITUDINAL_POLICY_REQUIRED`. `rehab -> return_or_rebuild` plus non-diagnostic `pain_aware_return` context.
