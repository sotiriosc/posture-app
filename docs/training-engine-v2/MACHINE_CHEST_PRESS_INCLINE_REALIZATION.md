# Machine Chest Press Incline Realization

Status: `ONE_IDENTITY_EXACT_MACHINE_ANGLE_REALIZATION`

`machine-chest-press` remains the sole production identity. `flat-machine` requires exact `chest_press`; `fixed-machine-incline` requires exact `incline_chest_press`. A commercial-gym label, generic selectorized-machine capability, or flat chest-press machine cannot satisfy the incline realization.

Machine geometry and angle are recorded as realization truth. No incline-machine exercise row, brand identity, flat-machine equivalence, upper-chest guarantee, scoring bonus, or automatic angle rotation is admitted.
