# Controlled Owner Current Product Invariance

No current Questionnaire, Results, Program, Session, Progress, Account page implementation, local draft store,
Program generation path, or Product training-state route was modified. The Account addition is a server layout
whose denied branch returns the original children exactly. Owner writes target only the nine `owner_v2_*` tables.

The owner Product adapter reads structured facts as proposals and persists only references and fingerprints.
It does not write Product state, infer exact minutes or loads, or treat legacy logs as V2 Performance.
