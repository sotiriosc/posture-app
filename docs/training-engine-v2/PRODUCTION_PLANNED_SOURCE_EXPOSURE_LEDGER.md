# Production Planned Source Exposure Ledger

Generated deterministically from the inactive production Post-Prescription Week Validator kernel.

Expected/observed/unique source events: `860/860/860`. Identity errors: `0`.

One row is one `sourceExposureEventId`. Objective, need, muscle, action, role, block, set, side, and revision views do not duplicate the row.
