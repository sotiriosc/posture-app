# Session Practice Option Availability

`SESSION_PRACTICE_OPTION_AVAILABILITY@1.0.0` reports the closed availability vocabulary, structured
reason codes, source evidence, materiality, and `fallbackApplied: false`. Full requires executable
source truth. Lighter requires a materially lower valid realization. Recovery requires a coherent
explicit support realization. Current UI does not consume these states.
