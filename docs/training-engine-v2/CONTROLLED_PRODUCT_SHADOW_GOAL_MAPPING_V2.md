# Controlled Product Shadow Goal Mapping V2

`Get stronger -> strength`; `Build muscle -> hypertrophy`; `Improve posture` and `Improve posture and movement -> posture_and_movement_quality`.

`Reduce pain` creates `pain_aware_return` context and requires a primary outcome. `General fitness` requires a purpose bundle. Athletic Performance labels require structured follow-up.

`Improve fitness and stamina` requires an explicit focus; systemic conditioning returns `SYSTEMIC_CONDITIONING_POLICY_REQUIRED`. Unknown labels are never guessed.
