# Controlled Owner Active Program Ownership

Owner-scoped modes are `legacy` and `v2_owner`. Initial mode is `legacy`; application selects `v2_owner`; rollback restores `legacy`. The pointer is persisted server-side and is neither global nor browser authority.

Design-only. Owner delivery and Product activation counts remain `0`. Exact next dependency: `CONTROLLED_OWNER_ACCOUNT_GET_STRONGER_GOAL_DELIVERY_IMPLEMENTATION_V1_AUTHORIZATION`.
