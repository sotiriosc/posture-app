# Phase 9 — Starting Requirements (evidence-backed only)

Phase 8 stops here. Do **not** begin Phase 9 in this branch/PR.

## Evidence-backed next work

1. **Live React visual QA** — Capture first-reveal screenshots from authenticated `/results` (`dashboardLevel === 1`) and gyms parity pixels once seed helpers exist; replace fixture-proxy honesty gaps.
2. **Shared UI package (optional)** — If product wants to end `DUPLICATE_IMPLEMENTATION` mirrors for plan-reveal / session-start, introduce `packages/ui` with proven runtime + tests (out of Phase 8 scope).
3. **Body-map** — Production consideration only after a polished illustration pass clearly beats pills (Phase 8 gate **FAIL**). Until then keep `/dev/body-map` demo-only; do not persist map selections.
4. **Gyms Interface** — Only if product explicitly wants sectionVisibility in gyms; currently intentional absence.
5. **Adaptation delight across active workout** — Spec lists this as Phase 9 territory; do not start animation system work here.
6. **Deferred cleanup list** — See inventory deferred findings; delete nothing without §J proof.

## Non-goals carried forward

- No program-generation / template version bump from presentation polish alone
- No production body-map persistence without an explicit later phase approval
- No opportunistic dead-code deletion mixed into UX work
